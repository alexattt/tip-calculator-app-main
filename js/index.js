window.addEventListener('load', function () {

  // this is how readonly input field placeholder value is changed
  // let tipPerPerson = this.document.getElementsByName('tip-per-person');
  // tipPerPerson[0].placeholder = "$5.25"

  // let totalPerPerson = this.document.getElementsByName('total-per-person');
  // totalPerPerson[0].placeholder = "$30.76"
  
})